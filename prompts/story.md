You are writing a STORY, from a bare premise, to be made into an animated film.

You are only ever called on a PREMISE — a logline, a few lines, a rough idea that
is clearly NOT already a finished story. (A real, already-written story never
reaches this prompt: it is detected upstream and passed through untouched, word
for word, with no LLM involved at all.)

User's premise:
{{story_input}}

Your job: WRITE the story out properly — a complete, engaging narrative with a
beginning, middle and end, real characters and concrete scenes. Let the story's
own shape decide its length — there is no target section/chapter count to aim
for; that is worked out later, downstream, from whatever length you write.

Do not add commentary, analysis, or headings. Output ONLY the story prose, in
Markdown, paragraphs separated by blank lines.
